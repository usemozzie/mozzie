{
  "name": "mozzie",
  "version": "0.1.6",
  "private": true,
  "description": "Local-first desktop app for AI agent orchestration with git integration",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/usemozzie/mozzie.git"
  },
  "scripts": {
    "dev": "turbo run dev",
    "dev:app": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck"
  },
  "devDependencies": {
    "concurrently": "^8.2.0",
    "turbo": "^2.0.0",
    "typescript": "^5.5.0"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "packageManager": "pnpm@9.0.0",
  "dependencies": {
    "node-pty": "^1.1.0"
  }
}
